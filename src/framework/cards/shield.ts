import { Card } from '../structures/Card';

export default new Card({
  id: 'shield',
  name: 'Shield',
  description: 'Block damage for a specific turn.',
  quantity: 6,

  execute(ctx) {
    // WIP
  },
});
